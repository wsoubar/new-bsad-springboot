spring:
  application:
    name: bsad-open-framework
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:mysql://localhost:3306/bsad_demo?useSSL=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
    show-sql: true

server:
  port: 8080

# Enable caching with Ehcache
spring:
  cache:
    type: jcache

# Apache CXF base path for SOAP services
cxf:
  path: /services
